---
version: "3.9"
services:
  dashboard:
    image: python:3.11-slim
    working_dir: /app
    command: ["./start.sh"]
    volumes:
      - ./:/app:delegated
    ports:
      - 8501:8501
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
